extern crate rayon;use std::io::*;use rayon::prelude::*;fn main(){let x="extern crate rayon;use std::io::*;use rayon::prelude::*;fn main(){let x=";let y="(0..8).into_par_iter().for_each(|i|{for j in 0..93750{BufWriter::new(std::fs::File::create(format!(\"{}{}\",i,j)).unwrap()).write(format!(\"{}{0:?};let y={:?};{1}\",x,y).as_bytes());}});}";(0..8).into_par_iter().for_each(|i|{for j in 0..93750{BufWriter::new(std::fs::File::create(format!("{}{}",i,j)).unwrap()).write(format!("{}{0:?};let y={:?};{1}",x,y).as_bytes());}});}