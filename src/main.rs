extern crate rayon;use std::io::{BufWriter, Write};use rayon::prelude::*;fn main(){let x="extern crate rayon;use std::io::{BufWriter, Write};use rayon::prelude::*;fn main(){let x=";let y="let t=8;let n=750_000/t;(0..t).into_par_iter().for_each(|i|{for j in 0..n{BufWriter::new(std::fs::File::create(format!(\"test/{}\",(i*n)+j)).unwrap()).write(format!(\"{}{:?};let y={:?};{}\",x,x,y,y).as_bytes()).unwrap();}});}";let t=8;let n=750_000/t;(0..t).into_par_iter().for_each(|i|{for j in 0..n{BufWriter::new(std::fs::File::create(format!("test/{}",(i*n)+j)).unwrap()).write(format!("{}{:?};let y={:?};{}",x,x,y,y).as_bytes()).unwrap();}});}